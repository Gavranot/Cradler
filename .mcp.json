{
  "mcpServers": {
    "obsidian": {
      "type": "http",
      "url": "http://${OBSIDIAN_MCP_HOST:-localhost}:3002/mcp",
      "headers": {
        "Authorization": "Bearer ${OBSIDIAN_MCP_TOKEN}"
      }
    }
  }
}
